// 数组
let arr: (string | number)[] = ['cl', 'xupt', 18];

// 元组
// 如果不按照对应的顺序定义类型就会报错
let tuple: [string, string, number] = ['cl', 'xupt', 18];

let tuples: [string, string, number][] = [['cl', 'xupt', 18]]
